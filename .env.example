# Variáveis de ambiente para conexão com o banco Supabase

# Host do banco de dados (endpoint do Supabase)
DB_HOST=seu-host-do-supabase.supabase.co

# Porta do banco (padrão Postgres é 5432)
DB_PORT=5432

# Usuário do banco (geralmente 'postgres')
DB_USER=postgres

# Senha do banco (encontrada no painel do Supabase)
DB_PASSWORD=sua-senha-do-supabase

# Nome do banco (geralmente 'postgres' ou o nome do seu projeto)
DB_NAME=postgres
